// Example type definitions
export interface User {
  id: string;
  username: string;
  email: string;
}

export interface Todo {
  id: string;
  title: string;
  completed: boolean;
  userId: string;
}

// Add more types as needed
